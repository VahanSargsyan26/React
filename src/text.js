function MyFunc(props) {
    return (
        <div>
            <h1>{props.fullName}</h1>
            <h2>{props.name}</h2>
            <h3>{props.age}</h3>
        </div>
    )
}

export default MyFunc;